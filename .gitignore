.DS_Store
/.build
/Packages
xcuserdata/
DerivedData/
.swiftpm/configuration/registries.json
.swiftpm/xcode/package.xcworkspace/contents.xcworkspacedata
.netrc
*.xcodeproj
.claude/
scripts/download-stats.sh

# Release signing credentials — never commit (RELIMPR #1).
scripts/.env

# Local DMG store that feeds `generate_appcast` delta computation. Large
# binaries; the generated appcast XML is committed, the DMGs are not.
/releases
